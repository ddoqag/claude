Bypass Permissions Mode

启用权限绕过模式，允许Claude在没有用户确认的情况下执行操作。

用法：
/bypass-permissions on  - 启用权限绕过
/bypass-permissions off - 禁用权限绕过

注意事项：
- ⚠️ 启用后，Claude将自动执行所有操作，不再询问确认
- 请仅在可信环境中使用此模式
- 可以随时使用 /bypass-permissions off 关闭